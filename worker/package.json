{
  "name": "worker",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch index.ts",
    "build": "esbuild index.ts --bundle --platform=node --outdir=dist --format=esm --external:puppeteer --external:axios"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22",
    "esbuild": "^0.25.2",
    "typescript": "^5"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "eventsource-parser": "^3.0.2",
    "node-color-log": "^12.0.1",
    "puppeteer": "^24.35.0"
  }
}
