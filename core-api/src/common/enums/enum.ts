export enum Role {
  ADMIN = 'admin',
  USER = 'user',
}

export enum WorkerName {
  SUBFINDER = 'subfinder',
  NAABU = 'naabu',
  DNSX = 'dnsx',
  HTTPX = 'httpx',
}

export enum JobStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  FAILED = 'failed',
  CANCELLED = 'cancelled',
}
